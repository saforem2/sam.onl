# TODO

## Sidebar

- [ ] Rewrite Sidebar.astro as a recursive `<ul>` tree that walks the page hierarchy dynamically, replacing the current hardcoded rendering paths (year-grouped, linked, subdir-grouped). This would support arbitrary nesting and simplify the component significantly.
