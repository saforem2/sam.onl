# Session Context

## User Prompts

### Prompt 1

Update the frontmatter of 7 slug-based blog posts to add `date` and `description` fields. Each file is under /Users/samforeman/projects/saforem2/sam.onl/web/src/pages/posts/.

For each file, read it first, then use the Edit tool to replace the existing frontmatter block. Keep all existing fields (`layout`, `title`) unchanged. Add the new fields AFTER `title`.

IMPORTANT: Use single quotes for all string values in the frontmatter to match the existing style.

Here are the exact edits:

1. `pos...

