In the codebase at /Users/samforeman/projects/saforem2/sam.onl, find:

1. The landing/home page file (likely index.astro or similar) and show its full content
2. The Layout.astro file used by the landing page (referenced in frontmatter)
3. Any other keyboard/vim navigation handlers that would be active on the landing page (NOT Doc.astro, which is for doc pages)

I need to understand:
- What layout wraps the landing page
- Whether that layout has its own keydown handlers
- What `<main>` element exists on the landing page
- What elements inside main have tabindex or are focusable (a[href], buttons, etc.)
- Whether there's any window-level j/k bootstrap handler on the landing page

Return the full file paths, relevant code sections with line numbers.