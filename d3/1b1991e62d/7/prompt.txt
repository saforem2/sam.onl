Explore the codebase at /Users/samforeman/projects/saforem2/sam.onl to find how the `j` and `k` key navigation works on the landing page. I need to understand:

1. The VimNavigation component (`web/src/components/landing/VimNavigation.astro`) - read its full content
2. Any JavaScript/TypeScript files that handle keyboard events for j/k navigation on the landing page
3. The scroll utility (`web/src/utils/scroll.ts`) - read its full content
4. Any other scripts in the landing page components that handle keyboard-based scrolling or link focusing

I'm trying to figure out if the `j`/`k` keys scroll between snap sections or cycle through focusable links, and whether changing `scroll-snap-type` from `proximity` to `mandatory` could have broken this behavior.

Read the full content of all relevant files and return them.