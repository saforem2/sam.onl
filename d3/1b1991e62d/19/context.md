# Session Context

## User Prompts

### Prompt 1

Search the codebase at /Users/samforeman/projects/saforem2/sam.onl/web/src/pages/ for internal links that reference webtui-related paths. I need to understand:

1. Find all links in .mdx/.astro/.md files that point to webtui-related content (CSS, theme, plugin packages)
2. Check what the actual directory structure is under web/src/pages/ for webtui content — is there a /webtui/ prefix directory?
3. Identify which links are broken because they're missing a /webtui/ prefix

Look for patterns li...

