# Session Context

## User Prompts

### Prompt 1

Explore the codebase at /Users/samforeman/projects/saforem2/sam.onl to find all theme/color-related CSS. I need to understand:

1. How light/dark themes are implemented (look for `[data-theme]`, `prefers-color-scheme`, `:root` color definitions)
2. What Catppuccin color variables are available (look for `--blue`, `--green`, `--mauve`, `--mantle`, `--crust`, `--surface0`, `--surface1`, `--surface2`, `--overlay0`, etc.)
3. The theme CSS files in `packages/` -- especially any Catppuccin-related ...

