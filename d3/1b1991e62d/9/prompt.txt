Explore the codebase at /Users/samforeman/projects/saforem2/sam.onl to understand the root/index page and its snap scroll implementation. I need to find:

1. The root/index page (likely `web/src/pages/index.astro` or `web/src/pages/index.mdx`)
2. The layout file used by the root page (look for Layout.astro or similar)
3. Any CSS related to scroll snapping (search for `scroll-snap`, `snap`, `overscroll`)
4. The "status line" component (the bar at the bottom showing "main sam.onl utf-8 Top")
5. Any CSS related to the status line positioning

Read the full content of each relevant file you find. Return:
- The exact file paths
- The full content of the root page
- The full content of the layout file
- Any CSS rules related to scroll-snap behavior
- The status line component file and its CSS
- How the page sections are structured (what creates the snap points)