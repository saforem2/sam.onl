---
/**
 * Speaker notes for a slide. Hidden in the main deck; visible only in
 * the speaker popup window (opened with `s`). Place inside a <Slide>.
 */
---

<aside is-="notes">
    <slot />
</aside>
